# Zero -> AGI : Python labs
#
# Labs 01, 02, 03, 04, 07 are PURE PYTHON and need nothing from this file.
# Labs 05 and 08 need numpy.
# Lab 06 (the tiny GPT) is the only one that needs PyTorch, and the CPU-only
# build is completely fine -- the --quick preset trains in about a minute on a
# laptop CPU. See https://pytorch.org/get-started/locally/ for the exact
# install command for your machine if the line below picks the wrong wheel.

numpy>=1.26
torch>=2.2
